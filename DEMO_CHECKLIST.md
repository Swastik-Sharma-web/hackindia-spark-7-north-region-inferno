# TrustWork X Demo Checklist

1. Set `NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID`, `NEXT_PUBLIC_CHAIN_ID`, `DATABASE_URL`, `GOOGLE_AI_STUDIO_API_KEY`, `PINATA_API_KEY`, `PINATA_SECRET_KEY`, `PRIVATE_KEY`, `REPUTATION_CONTRACT_ADDR`, `ESCROW_CONTRACT_ADDR`, `JWT_SECRET`, and `POLYGONSCAN_API_KEY`.
2. Deploy `ReputationPassport` and `TrustWorkEscrow` to Polygon Mumbai.
3. Copy contract addresses into `.env`.
4. Run the API on Railway and the frontend on Vercel.
5. Rehearse wallet connect, skill challenge, passport mint, job escrow, and dispute flow.
6. Capture screenshots for every transaction hash and Polygonscan confirmation.
